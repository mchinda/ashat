import { LigneLivraisonEntity } from './ligne-livraison.entity';

describe('LigneLivraisonEntity', () => {
  it('should be defined', () => {
    expect(new LigneLivraisonEntity()).toBeDefined();
  });
});
